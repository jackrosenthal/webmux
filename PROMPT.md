Read SPEC.md and IMPLEMENTATION_PLAN.md.  Find the first unchecked item
(`- [ ]`) in the implementation plan.  Implement that ONE item only.

Before committing, ensure both pass:
- `bun run typecheck`
- `bun run build`

Fix any errors before proceeding.  Then mark the item complete (`- [x]`),
commit, and exit.
