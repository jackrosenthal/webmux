{
  "name": "webmux",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'TODO: Add dev script'",
    "build": "echo 'TODO: Add build script'",
    "typecheck": "bunx --bun tsc --noEmit -p tsconfig.server.json && bunx --bun tsc --noEmit -p tsconfig.client.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.8",
    "typescript": "^5.7.0"
  }
}
